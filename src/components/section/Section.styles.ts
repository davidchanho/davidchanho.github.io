import styled from "styled-components";
import { Element } from 'react-scroll';

export const SectionContainer = styled(Element)`
    height: 100vh;
    margin-bottom: 200px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
`;